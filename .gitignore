# R
.Rproj.user/
.Rhistory
.RData
.Ruserdata

# Quarto
/.quarto/
*_files/
*_cache/

# macOS
.DS_Store

# Claude Code local tooling (preview server config)
.claude/

# Note: docs/ and _freeze/ ARE committed on purpose.
# docs/  = the rendered site GitHub Pages serves
# _freeze/ = cached R results so the site builds fast and reproducibly

**/*.quarto_ipynb
